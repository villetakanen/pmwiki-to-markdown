{
  "name": "pmwiki-to-markdown",
  "version": "0.2.1",
  "scripts": {
    "build": "tsc && vite build",
    "start": "node dist/index.js",
    "check": "biome check --write",
    "test": "vitest",
    "test-run": "tsc && vite build && node dist/index.js -i test-materials -o test-results --webp"
  },
  "bin": {
    "pmwiki-to-markdown": "dist/index.js"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.8.7",
    "typescript": "^5.9.2",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "fs": "0.0.1-security"
  }
}
